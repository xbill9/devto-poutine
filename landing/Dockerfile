FROM nginx:1.27-alpine

# Serve on 8080 so Cloud Run's default container port works unchanged.
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY index.html styles.css app.js /usr/share/nginx/html/

EXPOSE 8080
